package com.nureddinelmas.kotlinoopproject

interface Instrument {
    // Java -> no body
    // Kotlin -> body

    fun info(){
        println("instrument info,we are here in the Instrument interface ")
    }
}